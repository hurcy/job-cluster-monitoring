bundle:
  name: job-cluster-monitoring

include:
  - resources/*.yml

variables:
  catalog:
    description: "Unity Catalog 카탈로그 이름"
    default: "hurcy"
  analytics_schema:
    description: "분석 결과 출력 스키마 (Spill 뷰 + Right-Sizing 테이블)"
    default: "test"
  workspace_id:
    description: "분석 대상 워크스페이스 ID"
    default: "984752964297111"
  warehouse_id:
    description: "대시보드용 SQL Warehouse ID"
    lookup:
      warehouse: "Shared Endpoint"

targets:
  dev:
    default: true
    mode: development
    workspace:
      host: https://adb-984752964297111.11.azuredatabricks.net
      profile: DEFAULT
